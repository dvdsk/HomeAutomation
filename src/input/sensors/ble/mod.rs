//use secure_ble::{connect_to_adapter, connect_to_node, authenticate_node, StreamCipher, subscribe, printvalues};

mod node_credentials;
use node_credentials::*;


pub fn init(){
    //let central = connect_to_adapter();

    //let mut bed_node = connect_to_node(central, node_bed::ADRESS, Duration::from_secs(100)).unwrap();
    //authenticate_node(&mut bed_node, node_bed::HMAC_KEY);
}